# NextAuth (Auth.js): set to "false" only if you need strict host checks (e.g. custom server hardening)
AUTH_TRUST_HOST=true

# Supabase — Project Settings → API: Project URL and anon (legacy) or publishable public key
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Prisma — same project as above; paste from Supabase → Project Settings → Database
# DATABASE_URL: "Connection pooling" / Transaction (port 6543) — add ?pgbouncer=true for Prisma
# DIRECT_URL:   "Direct connection" (db.<ref>.supabase.co:5432) for migrations — or if Prisma P1001
#   (can't reach db host), use "Session" pooler URI (port 5432 on *.pooler.supabase.com) from the dashboard
# Password with special characters must be URL-encoded in the connection string
DATABASE_URL=
DIRECT_URL=
